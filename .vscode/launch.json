{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run Current Test",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/IRdetection",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/IRdetection"
            },
            "justMyCode": false
        }
    ]
}
